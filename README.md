# Crestie Lineage v7 — GitHub 자동 저장

크레스티드 게코의 개체 정보·성장·교배·가계도와 사진을 관리합니다.

## 사용
- 노트북과 휴대폰에서 https://cre-flame.vercel.app 을 엽니다.
- 작성할 기기에서 상단 저장 상태를 눌러 기록 저장용 비밀번호를 한 번 입력합니다. API 키를 발급하거나 코드에 넣을 필요가 없습니다.
- 저장 버튼을 누르면 이 기기에 먼저 보관한 뒤 GitHub에 자동 저장합니다.
- **✓ GitHub 저장됨** 표시를 확인하면 다른 기기에서도 기록을 볼 수 있습니다.
- 다른 기기의 변경은 사이트를 열거나 화면에 돌아왔을 때, 또는 15초 주기로 확인합니다.
- 오프라인 기록은 브라우저에 유지하고 재접속 시 재시도합니다. 저장 대기 중에는 브라우저 데이터를 지우지 마세요.
- 같은 항목을 두 기기에서 다르게 수정하면 상단에서 충돌을 확인하고 사용할 내용을 선택합니다. 다른 항목의 변경은 자동으로 합칩니다.

## 저장 위치
- 정보: steo410/cre 저장소의 crestie-data 브랜치, data/cresties.json
- 사진: 같은 브랜치의 data/photos/
- 프로그램: main 브랜치
- 저장 연결 시험 파일: data/sync-check.json (실제 사육 정보 없음)

저장소가 공개되어 있어 기록과 사진도 공개됩니다. 작성만 비밀번호로 보호합니다. 비밀번호와 GitHub 연결 토큰은 GitHub에 저장하지 않습니다.

기존 브라우저 저장 키와 기록을 유지합니다. 저장 실패로 브라우저에 남은 기록은 새 서버 연결 시 자동 업로드합니다. 기존 Blob 저장소 파일을 삭제하지 않습니다. 이번 전환 시 Blob 데이터가 비어 있음을 확인했습니다.

## 서버
Vercel의 기존 CRESTIE_GITHUB_TOKEN과 CRESTIE_SYNC_KEY 설정을 사용합니다. 데이터 API는 GitHub 파일 SHA를 기준으로 조건부 저장하여 오래된 기기의 덮어쓰기를 방지합니다. 사진은 새 파일로 저장하여 기존 사진을 덮어쓰지 않습니다.
한글 비밀번호는 브라우저에서 SHA-256으로 변환하여 HTTP 헤더로 전달합니다. 이 값 역시 인증 정보이므로 로그에 출력하지 않습니다.

## 검증
node --test tests/*.test.js

Production 배포 시 실제 서버 설정으로 data/sync-check.json에 시험 기록을 저장한 뒤 다시 읽어 일치 여부를 확인합니다. 실패하면 새 버전을 배포하지 않습니다. 데이터 전용 브랜치는 Vercel 자동 배포를 끕니다.

## 사진첩과 가계부 (v7)
- 개체 등록·수정에서 사진 여러 장을 한 번에 선택할 수 있습니다. 기존 대표 사진도 유지합니다.
- 개체 목록의 사진 또는 사진첩의 사진을 누르면 크게 보고, 이전·다음 이동과 최대 400% 확대를 할 수 있습니다.
- 사진 업로드가 끝나고 GitHub 저장됨 표시가 나올 때까지 화면을 열어 두세요. 실패한 사진은 화면의 다시 저장 버튼으로 재시도합니다. 실패한 파일을 다시 시도하기 전에 브라우저를 닫으면 재선택이 필요합니다.
- 사육 · 거래 가계부에서 구매·판매, 분류, 금액, 날짜, 관련 개체와 메모를 기록합니다. 월별·분류별 필터, 합계, 수정·삭제와 CSV 다운로드를 제공합니다.
- 가계부의 금액·메모도 같은 공개 GitHub 저장소에 저장되므로 다른 사람이 볼 수 있습니다.
- JSON 백업은 사진 주소와 가계부를 포함합니다. 사진 파일 자체까지 따로 보관하려면 crestie-data 브랜치를 ZIP으로 내려받으세요.
- 이전 JSON을 복원할 때 파일에 사진첩이나 가계부 항목이 없으면 현재 해당 기록을 유지합니다. 파일에 해당 배열이 있으면 그 내용으로 복원합니다.
- 업데이트 후 노트북과 휴대폰의 페이지를 새로고침하세요. 이전 버전의 화면은 새 사진첩·가계부를 지우지 못하도록 서버 저장을 거부합니다.



## 모프 도감과 특징 선택
- 모프·특징 46개를 기본 무늬, 부분 특징, 점 무늬, 색상, 유전 모프, 조합 모프, 라인으로 나눠 안내합니다.
- 한글·영문·별칭 검색과 분류 필터, 각 카드의 구분·유전 설명을 제공합니다.
- 릴리화이트, 아잔틱, 릴리아잔틱은 각각 별도 항목입니다. 릴리 100% 헷 아잔틱도 비주얼 조합과 구분합니다.
- 개체 등록·수정의 ‘모프 · 특징 선택하기’에서 여러 항목을 선택합니다. 선택 결과는 기존 `customTraits`에 저장되며 개체 목록에 태그로 표시됩니다.
- 선택만으로 유전자형을 확정하거나 교배 확률에 추가하지 않습니다. 기존 표시 모프, 분양 표기와 유전 정보 입력란을 유지합니다.
- 기존 기록 형식과 저장 API는 그대로 사용합니다. 새 파일 3개도 정적 빌드에 포함합니다.
